# Layout modes; the active one is per workspace, set with workspace-set-layout.
[layout]
mode = "master"
gap = 4

[layout.scrolling]
default_extent_fraction = 0.5

[layout.master]
position = "right"
default_width_fraction = 0.5
new_becomes_master = true
