# Rendered by noctalia's theme templates; scoped to config/ so the nix modules stay tracked.
config/**/noctalia.*
config/**/themes/noctalia
.playwright-mcp/
result

# Rendered by DMS's matugen foot template into the tracked foot config dir.
config/foot/dank-colors.ini
